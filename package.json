{
  "name": "compare-property",
  "version": "1.0.0",
  "description": "Array sort compare util",
  "keywords": ["sort", "compare"],
  "author": "David Brockman Smoliansky",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover _mocha -- --reporter spec --ui bdd --slow 10 --check-leaks",
    "watch": "mocha --watch --reporter min --ui bdd --slow 10 --check-leaks",
    "clean": "rm -rf coverage"
  },
  "devDependencies": {
    "mocha": "~1.16.2",
    "should": "~2.1.1",
    "istanbul": "~0.2.2"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/dbrockman/compare-property.git"
  },
  "bugs": {
    "url": "https://github.com/dbrockman/compare-property/issues"
  },
  "homepage": "https://github.com/dbrockman/compare-property",
  "license": "MIT",
  "engines": {
    "node": ">= 0.8.0"
  }
}
